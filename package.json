{
  "name": "happy-birthday",
  "version": "2.1.0-beta.1",
  "description": "A configurable birthday microsite with a safe local generator for personalized celebrations.",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "serve": "rspack serve",
    "start": "rspack serve",
    "validate": "node scripts/validate-config.mjs",
    "create": "node scripts/create-site.mjs",
    "preview": "node scripts/preview-site.mjs",
    "check-dist": "node scripts/check-dist.mjs",
    "test": "node --test"
  },
  "author": "Tsingper",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@rspack/cli": "^2.1.8",
    "@rspack/core": "^2.1.8",
    "@rspack/dev-server": "^2.2.0"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "gsap": "^3.15.0"
  }
}
